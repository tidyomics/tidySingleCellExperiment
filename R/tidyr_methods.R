# 'nest()', 'unnest()', 'extract()', 'unite()', 'separate()', 'pivot_longer()'
# and 'pivot_wider()' are provided by tidySummarizedExperiment, which operates
# on the feature-by-sample tibble abstraction.
